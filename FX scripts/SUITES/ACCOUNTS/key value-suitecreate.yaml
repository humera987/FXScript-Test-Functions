name: create_KeyValueacc

authors:
  - Intesar Shannan Mohammed
  - Humera Sultana

type: SUITE

endpoint: /accounts

method: POST

auth: Default

testCases:
  - id: 1
    body:
      '{
        "name": "syeds",
        "accountType": "KeyValue",
        "value": "keyvalue1234"
      }'

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data.name == @Request.name"
  - "@Response.data.accountType == @Request.accountType"
  - "@Response.data.value == EMPTY"

severity: Major

category: Functional

tags:
   - V2

policies:
  logger: DEBUG
