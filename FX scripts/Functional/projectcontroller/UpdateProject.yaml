name: UpdateProject

authors:
  - Asiya Shaji
  - Humera Sultana

type: SUITE

init:
  - creategithubaccabstract
  - CreateProjectopenAPISpecAbstract

endpoint: /projects

method: PUT

auth: Default

headers:
  - 'Content-Type: application/json'

testCases:
  - id: 1
    body:
      '{
        "Id": "{{@CreateProjectopenAPISpecAbstract_Response.data.id}}",
        "org": {
            "Id": "{{@CreateProjectopenAPISpecAbstract_Response.data.org.id}}"
          },
        "name": "{{@Random | 5}}"
        "url": "https://github.com/humera987/FXScript-Test-Functions.git",
        "account": "{{@creategithubaccabstract_Response.data.id}}"
      }'

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"

cleanup:
  - DeleteProjectopenAPISpecAbstract
  - deletegithubaccabstract

severity: Major

category: Functional

tags:
- V2
