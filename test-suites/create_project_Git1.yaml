name: create_project_Git1

authors:
  - Asiya Shaji
  - Humera Sultana

type: SUITE

endpoint: /projects

method: POST

auth: Default

headers:
 - 'Content-Type: application/json'

testCases:
  - id: 1
    body:
      '{
        "name": "{{@Random | 3}}",
        "url": "https://13.56.210.25/v2/api-docs"
       }'

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data.name == @Request.name"
  - "@Response.data.url == @Request.url"

cleanup:
  - delete_project_Git

severity: Major

category: Functional

tags:
- V2